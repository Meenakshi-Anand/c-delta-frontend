<script>
  /*
    Quick way to get creativeQualities to JS.
  */
  // window.qualsJSON = <%== @creative_qualities.to_json %>

  // If you have skipped Part 1, please comment out the above window.qualsJSON ...
  // and uncomment this to generate the data you need for the React component(s):
  //
  window.qualsJSON = <%== mock_creative_quality_scores.to_json %>
  //
</script>

<!-- This is where the React app gets rendered -->
<div id="react-root">
  <!-- currently just rendering H2 title -->
</div>

<!-- <div class="row">
  <div class="col-md-12">
    <div class="row">
      <%= @creative_qualities.find_each do |creative_quality| %>
        <div class="col-md-4">
          <div class="panel panel-default">
            <div class="panel-heading">
              <%= creative_quality.name %>
            </div>

            <div class="panel-body">

              <p>
                <%= creative_quality.description %>
              </p>

              <% score = (creative_quality.raw_score.to_f / creative_quality.max_score * 100).round %>
              <% score = score.floor if score > 100 %>
              <% score = score.ceil if score < -100 %>
              <hr>
              <p>
                <strong>Your Score: <%= score%></strong>
              </p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div> -->
